import { createActionGroup, props } from "@ngrx/store";
import { User } from "../users-list.component";


export const UsersActions = createActionGroup({
    source: 'User',
    events: {
       'set': props<{users: User[]}>(),

       'edit': props<{ user: User }>(),

       'create': props<{ user: User }>(),

       'delete': props<{ id: number }>(),
    }
})

/* Разбор кода по частям
1. import { createActionGroup, props } from "@ngrx/store";
createActionGroup: Мы импортируем основную функцию-конструктор для нашей "панели управления".
props: Это функция-помощник, которая позволяет указать, какие данные (полезную нагрузку) будет нести с собой
каждая "кнопка" (Action).
2. export const UserActins = createActionGroup({ ... })
export const UserActins — мы создаем и сразу экспортируем (делаем доступной для других файлов) константу
UserActins. Эта константа и будет нашим "пультом управления".
createActionGroup({ ... }) — мы вызываем конструктор и передаем ему один объект для настройки.
3. source: 'User'
Это источник или категория для всех экшенов в этой группе.
Что это дает? NgRx автоматически использует это слово как префикс для всех ваших экшенов. Например, экшен set
 на самом деле будет иметь тип [User] set. Это невероятно удобно для отладки — вы всегда будете видеть в логах,
 какая часть приложения отправила команду.
4. events: { ... }
Это и есть набор "кнопок" на нашем пульте управления. events — это объект, где каждый ключ — это название
конкретного события (экшена).
Давайте посмотрим на каждую "кнопку":
'set': props<{users: User[]}>()
Что это: Команда "Установить полный список пользователей".
props<{users: User[]}>(): Говорит, что при нажатии этой "кнопки" (при вызове этого экшена) мы обязаны передать
 с ней данные — объект, у которого есть свойство users,
 и это свойство является массивом ([]) объектов типа User.
Пример использования: Когда вы загрузили пользователей с сервера и хотите положить их в хранилище (Store).
'edit': props<{ user: User }>()
Что это: Команда "Отредактировать одного пользователя".
props<{ user: User }>: Требует передать объект со свойством user, который является одним объектом типа User.
'create': props<{ users: User }>()
Что это: Команда "Создать одного нового пользователя".
props<{ users: User }>: Требует передать одного нового пользователя.
Кстати, здесь, вероятно, небольшая опечатка. Для создания одного пользователя логичнее было бы назвать
свойство user, а не users: 'create': props<{ user: User }>(), как и для редактирования.
Но суть от этого не меняется.
'delete': props<{ id: number }>()
Что это: Команда "Удалить пользователя".
props<{ id: number }>: Для удаления нам не нужен весь объект пользователя.
Достаточно передать его уникальный идентификатор (id), который является числом (number).
Что в итоге получается?
Эта одна конструкция создает объект UserActins, у которого есть свойства для каждого вашего экшена.
Теперь в другом файле (например, в компоненте или эффекте) вы можете легко вызывать эти команды:

 */
